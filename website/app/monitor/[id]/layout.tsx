export default function MonitorDetailLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <style>{`body { background: #fff !important; color: #0f0f0f; }`}</style>
      {children}
    </>
  )
}

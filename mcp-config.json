{
  "mcpServers": {
    "letsfg": {
      "command": "npx",
      "args": ["-y", "letsfg-mcp"],
      "env": {
        "LETSFG_API_KEY": "YOUR_API_KEY_HERE (optional — search works without it via cloud backend)"
      }
    }
  }
}
